'use strict';

describe('myApp.addTournament module', function() {

  beforeEach(module('myApp.addTournament'));

  describe('addTournament controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});